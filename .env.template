# =============================================================================
# n8n Produktions-Umgebungsvariablen Template
# =============================================================================
# Kopiere diese Datei zu .env und fülle die Werte aus
# cp .env.template .env

# =============================================================================
# DOMAIN & URL KONFIGURATION
# =============================================================================
# Deine Domain für n8n (ohne https://)
N8N_HOST=

# Vollständige Webhook URL (mit Domain)
WEBHOOK_URL=https:///

# =============================================================================
# N8N KONFIGURATION
# =============================================================================
N8N_PROTOCOL=https
N8N_PORT=5678
N8N_PROXY_HOPS=1

# Verschlüsselungsschlüssel für n8n (mindestens 32 Zeichen)
# Generiere mit: openssl rand -hex 32
N8N_ENCRYPTION_KEY=

# Zeitzone
GENERIC_TIMEZONE=Europe/Vienna

# =============================================================================
# DATENBANK KONFIGURATION
# =============================================================================
DB_TYPE=postgresdb
DB_POSTGRESDB_HOST=postgres
DB_POSTGRESDB_DATABASE=n8n
DB_POSTGRESDB_USER=n8n

# PostgreSQL Passwort (stark und sicher wählen)
# Generiere mit: openssl rand -base64 32
DB_POSTGRESDB_PASSWORD=

# =============================================================================
# OPTIONAL: ERWEITERTE KONFIGURATION
# =============================================================================
# N8N_BASIC_AUTH_ACTIVE=true
# N8N_BASIC_AUTH_USER=
# N8N_BASIC_AUTH_PASSWORD=

# Email für Let's Encrypt (empfohlen)
# CADDY_ACME_EMAIL=

# =============================================================================
# BACKUP KONFIGURATION
# =============================================================================
# Backup Verzeichnis auf dem Host
# BACKUP_DIR=/opt/n8n-backups

# Anzahl der zu behaltenden Backups
# BACKUP_RETENTION_DAYS=30